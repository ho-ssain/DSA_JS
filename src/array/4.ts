// concat
// this adds new elements in the array at the end of the array and returns the array.

let array5: number[] = [1, 2, 3];
let newArray5: number[] = array5.concat([4, 5]);
console.log(newArray5);
